spring:
  application:
    name: artifact.service

  datasource:
    url: jdbc:postgresql://localhost:5432/artifact_db
    username: aigerim
    password: password123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#  flyway:
#    enabled: true
#    locations: classpath:db/migration

server:
  port: 8083

jwt:
  secret: ThisIsAVeryLongSecretKeyForHS256AlgorithmThatMustBeAtLeast256Bits!!
  expiration-ms: 3600000